#spring.profiles.active=production
spring.cache.cache-names=vets

# https://learn.microsoft.com/en-us/azure/spring-apps/tutorial-managed-identities-key-vault?tabs=system-assigned-managed-identity -->
# You must add the key vault URL in the application.properties file as shown above. Otherwise, the key vault URL may not be captured during runtime.
spring.cloud.azure.keyvault.secret.property-sources[0].endpoint=${SPRING_CLOUD_AZURE_KEY_VAULT_ENDPOINT}
spring.cloud.azure.keyvault.secret.property-sources[0].credential.managed-identity-enabled=true
spring.cloud.azure.keyvault.secret.property-sources[0].credential.client-id=${VETS_SVC_APP_IDENTITY_CLIENT_ID}